{% extends 'base.html.twig' %}

{% block title %} {% endblock %}
{% block body %}
    <div class="container">
        <h4>Création d'une marque</h4>
        {% for message in app.flashes('succes') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}    

        <p>{{ form(form) }}</p>

        <div class="form-error">
            {{ form_errors(form)}}
        </div>


    </div>
{% endblock %}
